from app.blocks.block.block import header, divider


def team_is_back():
    return [
        # header(f'👥 Team back 💻 from {break_type}️'),
        header(f'👥 Team back to work 💻️'),
        divider()
    ]
